{
  "name": "brand-angular",
  "version": "1.0.0",
  "description": "Brand project using Angular",
  "repository": "https://github.com/gabrielacorrea/brand-angular",
  "license": "MIT",
  "private": "true",

  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "jasmine-core": "^2.3.4",
    "karma": "~0.12",
    "karma-chrome-launcher": "^0.1.4",
    "karma-jasmine": "^0.3.5",
    "karma-junit-reporter": "^0.2.2"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "postinstall": "bower install",

    "pretest": "npm install",
    "test": "node node_modules/karma/bin/karma start karma.conf.js"
  }
}
